{
  "name": "safekeys",
  "version": "1.0.0",
  "description": "Scans for secrets and adds them to a vault.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.598.0",
    "node-vault": "^0.10.2"
  },
  "devDependencies": {
    "@types/node": "^18.19.4",
    "@types/node-vault": "^0.9.11",
    "typescript": "^5.4.5"
  }
}
